<%= simple_form_for @category, url: url, local: true, html: { novalidate: true, class: 'needs-validation' }  do |f| %>
<%= f.input :name, required: true %>
<%= f.input :description, as: :text, required: true %>
<%= f.button :submit, required: true %>
<% end %>